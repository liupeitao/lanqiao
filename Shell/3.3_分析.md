```bash
  1 #! /bin/bash
  2  
  3 echo hello, $0 file will bakup $1 if exist otherwise create it
  4  
  5 filename=$1
  6 if [ -e $filename ];                                                                   
  7 then
  8     echo $filename  exist, bakup it
  9     cp $filename $filename.bak
 10 else
 11     echo $filename not exist i will create it
 12     touch $filename
 13 fi
 14 echo File test compete
 
  1 #! /bin/bash
  2  
  3 echo "hello, $0 file will bakup $1 if exist otherwise create it"
  4  
  5 filename=$1
  6 if [ -e "$filename" ];                                                                   
  7 then
  8     echo "$filename  exist, bakup it"
  9     cp "$filename $filename.bak"
 10 else
 11     echo "$filename not exist i will create it"
 12     touch "$filename"
 13 fi
 14 echo File test compete
```

